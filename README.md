# Scratch Binary 3 Compiler
Compiles a subset of Javascript into scratch's sb3 file format

## Building
Run: `go build`

## Running
Build and run: `./sb3c`, or skip building and run directly with: `go run .`

## Installing
Run: `go build && sudo cp sb3c /usr/bin`
